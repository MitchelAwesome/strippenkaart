{% extends 'base.html.twig' %}

{% block body %}
    <h1>Lid creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
    <button type="submit" class="btn btn-lg btn-success btn-block-xs" >Toevoegen</button>
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('lid_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
